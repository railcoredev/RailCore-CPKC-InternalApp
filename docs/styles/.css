/* ============================================
   RAILCORE WORKER APP v3.7 — GLOBAL THEME
   ============================================ */

:root {
  --railcore-orange: #ff6600;
  --railcore-orange-lit: #ff944d;
  --railcore-steel: #2f3e4f;
  --bg-dark: #050910;
  --bg-panel: #0c121c;
  --bg-panel-alt: #111b2c;
  --border-subtle: #2a3444;
  --text-main: #e3e9f4;
  --text-muted: #98a3b5;
  --accent-distance: #ffb470;
}

/* Universal baseline */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: system-ui, -apple-system, "SF Pro Text", Segoe UI, sans-serif;
}

body {
  background: radial-gradient(circle at top right, #1d355f 0%, #050910 60%, #000 100%);
  color: var(--text-main);
  padding: 12px;
}

.app-shell {
  max-width: 500px;
  margin: 0 auto;
  background: linear-gradient(145deg, #0c121c, #121a27);
  padding: 10px;
  border-radius: 14px;
  border: 1px solid var(--border-subtle);
  box-shadow: 0 0 30px rgba(0,0,0,0.6);
}

/* ============================================
   HEADER
   ============================================ */

.header {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 10px;
}

.logo {
  font-size: 28px;
  letter-spacing: 0.08em;
  font-weight: 900;
}

.logo .rail {
  color: var(--railcore-orange);
}

.logo .core {
  color: #c7d0dd;
}

.header-sub {
  font-size: 10px;
  letter-spacing: 0.12em;
  opacity: 0.75;
  text-transform: uppercase;
}

/* ============================================
   FILTER BLOCK (STATE SELECTION)
   ============================================ */

.filter-block {
  margin-bottom: 8px;
}

.field-label {
  font-size: 11px;
  letter-spacing: 0.1em;
  color: var(--text-muted);
  margin-bottom: 3px;
  text-transform: uppercase;
}

.multiselect {
  width: 100%;
  min-height: 70px;
  max-height: 150px;
  padding: 6px;
  border-radius: 8px;
  background: #070b12;
  border: 1px solid var(--border-subtle);
  color: var(--text-main);
  overflow-y: auto;
  font-size: 14px;
}

/* ============================================
   STICKY BAR
   ============================================ */

.sticky-bar {
  position: sticky;
  top: 0;
  z-index: 20;
  background: var(--bg-panel-alt);
  border: 1px solid var(--border-subtle);
  border-radius: 10px;
  padding: 8px;
  margin-bottom: 10px;
  box-shadow: 0 6px 8px rgba(0,0,0,0.4);
}

.sticky-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 6px;
}

.sticky-col {
  flex: 1;
  min-width: 140px;
}

.sticky-label {
  font-size: 9px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 2px;
}

.sticky-subdivision-select,
.sticky-inline input {
  width: 100%;
  padding: 6px 8px;
  border-radius: 6px;
  background: #080d15;
  border: 1px solid var(--border-subtle);
  color: var(--text-main);
  font-size: 14px;
}

.sticky-inline {
  display: flex;
  gap: 6px;
}

.viewmode-row label {
  font-size: 12px;
  display: flex;
  align-items: center;
  gap: 4px;
}

.buttons-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.btn {
  padding: 6px 14px;
  background: #141e2e;
  border-radius: 999px;
  border: 1px solid var(--border-subtle);
  color: var(--text-main);
  font-size: 13px;
  cursor: pointer;
}

.btn-primary {
  background: linear-gradient(145deg, #ff944d, #ff6600);
  border-color: var(--railcore-orange);
  color: #111;
  font-weight: bold;
}

/* ============================================
   TABS
   ============================================ */

.tabs {
  display: flex;
  background: var(--bg-panel);
  border-radius: 10px 10px 0 0;
  overflow: hidden;
  border: 1px solid var(--border-subtle);
  border-bottom: none;
}

.tab {
  flex: 1;
  text-align: center;
  padding: 8px 0;
  font-size: 14px;
  color: var(--text-muted);
  cursor: pointer;
  border-right: 1px solid var(--border-subtle);
}

.tab:last-child {
  border-right: none;
}

.tab.active {
  color: var(--text-main);
  background: var(--bg-panel-alt);
  box-shadow: inset 0 -3px 0 var(--railcore-orange);
}

/* ============================================
   RESULTS PANELS
   ============================================ */

.results-shell {
  border: 1px solid var(--border-subtle);
  border-top: none;
  background: rgba(3, 7, 14, 0.85);
  border-radius: 0 0 10px 10px;
}

.results-panel {
  padding: 12px;
  max-height: 550px;
  overflow-y: auto;
}

/* ============================================
   CROSSING FORMAT — EXACT 3-LINE BLOCK
   ============================================ */

.result-block {
  margin-bottom: 14px;
  line-height: 1.35;
}

.result-crossing {
  font-family: ui-monospace, SF Mono, Menlo, Consolas, monospace;
  font-size: 15px;
}

.result-distance {
  font-family: ui-monospace, SF Mono, Menlo, Consolas, monospace;
  font-size: 13px;
  color: var(--accent-distance);
  padding-left: 18px;
}

/* Blank line between blocks handled by margin */

/* ============================================
   SIDINGS FORMAT
   ============================================ */

.siding-header {
  font-size: 15px;
  font-weight: bold;
  margin-bottom: 4px;
}

.siding-range {
  font-size: 13px;
  color: var(--text-muted);
  margin-bottom: 10px;
}

/* ============================================
   TRACK LENGTH FORMAT
   ============================================ */

.tracklengths-header {
  padding: 10px;
  border-bottom: 1px solid var(--border-subtle);
}

.input-select {
  padding: 6px;
  background: #080d15;
  border: 1px solid var(--border-subtle);
  color: var(--text-main);
  border-radius: 6px;
}

.track-row {
  display: flex;
  justify-content: space-between;
  padding: 6px 4px;
  border-bottom: 1px solid #1c2635;
}

.track-row:last-child {
  border-bottom: none;
}

/* ============================================
   FOOTER VERSION
   ============================================ */

.footer-version {
  margin-top: 8px;
  text-align: right;
  font-size: 10px;
  color: var(--text-muted);
  letter-spacing: 0.12em;
}

/* ============================================
   MOBILE OPTIMIZATION
   ============================================ */

@media (max-width: 400px) {
  .sticky-row {
    flex-direction: column;
  }
}
